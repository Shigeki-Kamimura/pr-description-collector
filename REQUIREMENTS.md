# 要件定義: PR Description Collector

## 背景 / 目的
- PR の description（Markdown 形式）に含まれるチェックリストを解析し、進捗（完了率）と各項目の状態を可視化する。 
- レビューや進捗共有のために、チェック済み/未チェックの一覧と行番号をすぐ確認できるようにする。 

## 利用者 / 想定ユースケース
- チーム全体: チェックリストの共有と PR 内容の保管を通じて、チーム開発の円滑化と過去の開発をさかのぼれるようにする。 

## スコープ
### 対象範囲（In Scope）
- Markdown のチェックリスト（`- [ ]` / `- [x]` / `* [ ]` / `1. [x]` など）を解析する。 
- 解析結果の一覧表示（チェック状態、項目テキスト、行番号）。 
- 合計数 / 完了数 / 完了率の表示。 
- ブラウザ上でテキストを入力し、結果を表示する UI。 
- レビュー完了後に、PR description を JSON として保存し、画像と合わせて OneDrive に保存する。 

### 非対象範囲（Out of Scope）
- PR API（GitHub/GitLab 等）との連携や自動取得。 
- Markdown の完全パーサー実装（チェックリスト以外の要素解析）。 
- 複数 PR や履歴の保存・共有。 
- GitHub 上での進捗管理やレビュー時のチャット。 

## 機能要件
1. **チェックリスト解析**
   - 入力は Markdown テキスト。 
   - `- [ ]`, `- [x]`, `* [ ]`, `1. [x]` 形式のリスト項目を検出する。 
   - `x` / `X` のチェックを完了として扱う。 
   - 各項目について以下を返す: 
     - `checked`: 完了かどうか 
     - `text`: チェックボックス後のテキスト（前後空白は除去） 
     - `line`: 入力内の行番号（1 始まり） 

2. **集計**
   - `total`: 検出された項目数 
   - `checked`: 完了項目数 
   - `percent`: 完了率（四捨五入の整数） 

3. **UI 表示**
   - 入力フォーム（テキストエリア）で Markdown を入力できる。 
   - 解析ボタンで結果を表示する。 
   - 結果には以下を表示する: 
     - 合計 / 完了数 / 完了率 
     - 各項目のチェック状態、テキスト、行番号 

4. **アーカイブ保存**
   - レビュー完了後のタイミングで、PR description の JSON と画像を OneDrive に保存する。 
   - チェックリスト解析結果は JSON に含める。 

## 非機能要件
- **速度**: 数十〜数百行程度の入力で即時に解析できる。 
- **可用性**: ローカル環境で動作（サーバーサイドレンダリング対応）。 
- **可読性**: UI は最小限でも、結果の視認性（チェック状態・行番号）が確保される。 
- **保管性**: OneDrive 上に JSON と画像が保存されること。 

## 入出力仕様
### 入力
- Markdown 形式のテキスト（任意の文字列）。 

### 出力
- チェックリスト項目の配列と集計値。 
- OneDrive に保存する JSON と画像。 

## 受け入れ基準
- チェックリストを含む Markdown を入力すると、項目が正しく抽出される。 
- `- [x]` と `- [X]` は完了扱いとなる。 
- 解析結果に行番号が表示される。 
- 合計数 / 完了数 / 完了率が表示される。 
- レビュー完了後、PR description の JSON と画像が OneDrive に保存される。 

## 既存実装との対応
- 解析処理は `parseChecklist` で行い、集計は `summarize` で計算する。 
- UI は `Index` ルートでフォーム入力と結果表示を行う。 
