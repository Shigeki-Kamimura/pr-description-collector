# 要件定義: PR Description Collector

## 背景 / 目的
- PR の description（Markdown 形式）に含まれるチェックリストを解析し、各項目の状態を可視化する。 
- レビューの完了時にディスクリプション全体とエビデンス画像をを保存し、過去の開発をさかのぼれることでチーム開発の円滑化を図る

## 利用者 / 想定ユースケース
- レビュアー: コード、チェックリストのレビュー完了後ディスクリプションの中にあるチェックリストとエビデンス画像を保存する

## スコープ
### 対象範囲（In Scope）
- Markdown のチェックリスト（`- [ ]` / `- [x]` / `* [ ]` / `1. [x]` など）を解析する。 
- ブラウザでマークダウンファイルと同様の形でテキストが表示される 
- レビュー完了後、保存ボタンを押すとPR description を JSON として保存し、画像と合わせて OneDrive に保存する。 

### 非対象範囲（Out of Scope）
- PR API（GitHub/GitLab 等）との連携や自動取得。  
- 複数 PR や履歴の保存・共有。 
- GitHub 上での進捗管理やレビュー時のチャット。 

## 機能要件
1. **チェックリスト解析**
   - Json本体は Markdown形式のテキスト。 
   - `- [ ]`, `- [x]`, `* [ ]`, `1. [x]` 形式のリスト項目を検出する。
   - `x` / `X` のチェックを完了として扱う。
   - 各項目について以下を返す: 
     - `checked`: 完了かどうか 
     - `text`: チェックボックス後のテキスト（前後空白は除去） 
     - `line`: 入力内の行番号（1 始まり） 

2. **UI 表示**
   - 解析ボタンで結果を表示する。
   - 保存ボタンでJsonを保存する
   - 画像はJson内にあるimgタグなど画像リンクをパースしダウンロードする
   - ボタン押下後、ボタン配置箇所の下部にディスクリプションを表示
   - ディスクリプションの下部に「チェックリスト項目」「エビデンス画像」「チェックリストの結果コメント」をカード形式でまとめて表示
   - レスポンシブ対応(Android, iPhone, iPad mini, iPad, iPad pro, PC）

4. **アーカイブ保存**
   - レビュー完了後のタイミングで、PR description の JSON と画像を OneDrive に保存する。 
   - チェックリスト解析結果は JSON に含める。 

## 非機能要件
- **速度**: 数十〜数百行程度の入力で即時に解析できる。 
- **可用性**: ローカル環境で動作（サーバーサイドレンダリング対応）。 
- **可読性**: UI は最小限でも、結果の視認性（チェック状態・行番号）が確保される。 
- **保管性**: OneDrive 上に JSON と画像が保存されること。 

## 入出力仕様
### 入力
- 入力は本アプリでは行わない。

### 出力
- プルリクエストのディスクリプション、チェックリストとエビデンスのカード 
- OneDrive に保存する JSON と画像。 

## 受け入れ基準
- チェックリストを含む Markdown を入力すると、項目が正しく抽出される。 
- `- [x]` と `- [X]` は完了扱いとなる。 
- 解析結果に行番号が表示される。 
- 合計数 / 完了数 / 完了率が表示される。 
- レビュー完了後、PR description の JSON と画像が OneDrive に保存される。 

## 既存実装との対応
- 解析処理は `parseChecklist` で行う。
- UI は `Index` ルートでフォーム入力と結果表示を行う。 
