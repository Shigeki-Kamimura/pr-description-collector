# UI仕様: チェックリスト表示カード

## 目的
- PR description のチェックリスト結果とエビデンス画像を、レビュー完了後に確認しやすく表示する。

## 対象範囲
- チェックリスト表示カードのみ。
- フォーム入力や取得ボタンなどは本仕様の対象外。

## 前提
- 入力は GitHub API から取得した PR の `body` を解析した結果を用いる。
- 画像は OneDrive に保存され、表示はファイル名と関連情報で行う（画像単体のビューは提供しない）。

## カード構成
- タイトル: 「チェックリスト結果」
- サブセクション: 「チェックリスト項目」
- サブセクション: 「エビデンス画像」
- サブセクション: 「チェックリストの結果コメント」

## 表示ルール
- 各チェックリスト項目は 1 行で表示する。
- 表示内容は `checked`（完了/未完了）、`text`、`line` を含む。
- 完了は視覚的に区別する（例: チェックアイコンや色の差）。
- 未完了は警告的に見えるが過度に強調しない。
- 項目数が多い場合は縦スクロール可能とする。
- カードのテキスト配置は以下の通りとする。
- カード上部の見出しに「チェックリストタイトル」を表示する。
- 見出し直下の本文に「テスト結果のテキスト」を表示する。
 - 空データ時はカード表示領域に「チェックリストがありません」を赤太文字で表示する。
 - エラー表示もカード表示領域に赤太文字で表示する。

## エビデンス画像の表示
- 画像は保存済みの連番ファイル名で表示する（例: `1.png`）。
- 可能であれば `originalFilename` と `sourceUrl` を補足情報として表示する。
- 画像の一覧はテキスト中心で、画像のサムネイルは表示しない。
 - カードの画像領域にはエビデンス画像を配置する。

## コメント
- 「チェックリストの結果コメント」は任意入力で、未入力の場合は空欄表示。
- 本カード内で議論や調整は行わない。

## レスポンシブ
- 320px から現行の iPhone、iPad 各シリーズ、ラップトップまでを想定してブレイクポイントを用意する。
- 厳密なデザイン指定は行わず、レイアウト崩れを防ぐための最小限の設計として扱う。
- PC とモバイルでカード幅が変わるだけのシンプルなレイアウト。
- モバイル時は各サブセクションを縦に並べる。
- ブレイクポイント（幅）:
  - 320px（小型スマホ）
  - 375px（標準 iPhone）
  - 414px（大型 iPhone）
  - 768px（iPad）
  - 1024px（iPad Pro / 小型ラップトップ）
  - 1280px（ラップトップ）

## 非対応
- 画像単体の閲覧 UI。
- チェックリスト項目と画像の手動紐付け UI。
